package com.quoteflow.subscription;

/**
 * Stable feature keys for entitlement checks.
 * Only implemented features are enforced; others are definitional for future use.
 */
public enum FeatureKey {
	CUSTOMERS,
	QUOTATIONS_MONTHLY,
	INVOICES_MONTHLY,
	REMOVE_QUOTEFLOW_BRANDING,
	MULTI_USER,
	ADVANCED_REPORTS,
	EMAIL_SENDING,
	AI_ASSISTANT
}
